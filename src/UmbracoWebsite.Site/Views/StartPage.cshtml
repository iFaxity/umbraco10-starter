@using Models = Umbraco.Cms.Web.Common.PublishedModels;
@inherits AdvaniaViewPage<Models.StartPage>
@{
	Layout = "~/Views/Layouts/_Site.cshtml";
}

<h2>@Model.Header</h2>

<ul>
    @foreach (var tag in Model.Tags)
    {
        <li>@tag</li>
    }
</ul>
